import math

def SNT(n):
    if n < 2: return False
    for i in range(2, int(n**0.5)+1):
        if n % i == 0: return False
    return True

def strongSNT():
    n = int(input("Nhap n:"))

    for i in range(2, n):
        for j in range(2, i):
            if SNT(j) == True:
                if i % j == 0 and i % int(j**2) == 0: print(i)

strongSNT()